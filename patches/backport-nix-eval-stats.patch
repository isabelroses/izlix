From d21df97e4874bc11f3adb90f1299b642a1e2174c Mon Sep 17 00:00:00 2001
From: isabel <isabel@isabelroses.com>
Date: Sun, 10 May 2026 16:49:09 +0100
Subject: [PATCH] libexpr: backport nix eval stats
---
 lix/libexpr/eval.cc    | 46 +++++++++++++++++++++++++++++++++++++++---
 lix/libexpr/nixexpr.cc |  2 ++
 lix/libexpr/nixexpr.hh | 18 +++++++++++++++--
 3 files changed, 61 insertions(+), 5 deletions(-)

diff --git a/lix/libexpr/eval.cc b/lix/libexpr/eval.cc
index e9d24f941..fe099a6b9 100644
--- a/lix/libexpr/eval.cc
+++ b/lix/libexpr/eval.cc
@@ -163,6 +163,16 @@ static void * oomHandler(size_t requested)
     throw std::bad_alloc();
 }
 
+/* Number of GC cycles that had completed by the time the collector was
+   initialized. Used to make `gc.cycles` in the stats output relative to
+   our process rather than to the GC's internal counter. */
+static GC_word gcCyclesAfterInit = 0;
+
+size_t getGCCycles()
+{
+    return static_cast<size_t>(GC_get_gc_no()) - static_cast<size_t>(gcCyclesAfterInit);
+}
+
 #endif
 
 Symbol getName(const AttrName & name, EvalState & state, Env & env)
@@ -196,8 +206,15 @@ void initLibExpr()
        there. */
     GC_set_no_dls(1);
 
+    /* Enable perf measurements before GC_INIT() so that
+       GC_get_full_gc_total_time() actually accumulates. Without this call
+       it always returns 0, making `time.gc` in NIX_SHOW_STATS useless. */
+    GC_start_performance_measurement();
+
     GC_INIT();
 
+    gcCyclesAfterInit = GC_get_gc_no();
+
     // Enable parallel marking
     GC_start_mark_threads();
 
@@ -1976,6 +1993,11 @@ void Evaluator::printStatistics()
 #if HAVE_BOEHMGC
     GC_word heapSize, totalBytes;
     GC_get_heap_usage_safe(&heapSize, 0, 0, 0, &totalBytes);
+    /* `GC_get_full_gc_total_time` returns ms; convert to seconds. It only
+       counts time spent in non-incremental (full) collections, so we report
+       it under different keys depending on the collector mode. */
+    double gcFullOnlyTime = GC_get_full_gc_total_time() * 0.001;
+    auto gcCycles = getGCCycles();
 #endif
 
     auto outPath = getEnv("NIX_SHOW_STATS_PATH").value_or("-");
@@ -1984,20 +2006,37 @@ void Evaluator::printStatistics()
         fs.open(outPath, std::fstream::out);
     JSON topObj = JSON::object();
     topObj["cpuTime"] = cpuTime;
+    topObj["time"] = {
+        {"cpu", cpuTime},
+#if HAVE_BOEHMGC
+        {GC_is_incremental_mode() ? "gcNonIncremental" : "gc", gcFullOnlyTime},
+        {GC_is_incremental_mode() ? "gcNonIncrementalFraction" : "gcFraction",
+         cpuTime > 0 ? gcFullOnlyTime / cpuTime : 0.0},
+#endif
+    };
     topObj["envs"] = {
         {"number", mem.nrEnvs},
         {"elements", mem.nrValuesInEnvs},
         {"bytes", bEnvs},
     };
+    topObj["nrExprs"] = Expr::nrExprs.load();
     topObj["list"] = {
         {"elements", mem.nrListElems},
         {"bytes", bLists},
         {"concats", stats.nrListConcats},
     };
-    // reported for compatibility, even though we no longer allocate these on the heap
+    /* Unlike upstream Nix, Lix never allocates `Value`s as standalone heap
+       objects -- they live inline inside Envs, Attrs, and List elements.
+       So we report the total number of Value slots that have been
+       allocated across those containers. The byte count overlaps with the
+       per-container `bytes` totals above (the Values are physically part
+       of those containers); we report it anyway so the field is comparable
+       to upstream's output and so `Value` size differences (Lix uses an
+       8-byte tagged pointer vs. upstream's 16+ bytes) are observable. */
+    uint64_t nrValues = mem.nrValuesInEnvs + mem.nrListElems + mem.nrAttrsInAttrsets;
     topObj["values"] = {
-        {"number", 0},
-        {"bytes", 0},
+        {"number", nrValues},
+        {"bytes", nrValues * sizeof(Value)},
     };
     topObj["symbols"] = {
         {"number", symbols.size()},
@@ -2025,6 +2064,7 @@ void Evaluator::printStatistics()
     topObj["gc"] = {
         {"heapSize", heapSize},
         {"totalBytes", totalBytes},
+        {"cycles", gcCycles},
     };
 #endif
 
diff --git a/lix/libexpr/nixexpr.cc b/lix/libexpr/nixexpr.cc
index c31e68141..8ac361e28 100644
--- a/lix/libexpr/nixexpr.cc
+++ b/lix/libexpr/nixexpr.cc
@@ -9,6 +9,8 @@
 
 namespace nix {
 
+std::atomic<uint64_t> Expr::nrExprs{0};
+
 ExprBlackHole eBlackHole;
 
 static Env nullEnv;
diff --git a/lix/libexpr/nixexpr.hh b/lix/libexpr/nixexpr.hh
index a2f5bfbf3..49da50df4 100644
--- a/lix/libexpr/nixexpr.hh
+++ b/lix/libexpr/nixexpr.hh
@@ -1,6 +1,8 @@
 #pragma once
 ///@file
 
+#include <atomic>
+#include <cstdint>
 #include <map>
 #include <memory>
 #include <vector>
@@ -111,13 +113,25 @@ struct Expr
 protected:
     Expr(Expr &&) = default;
     Expr & operator=(Expr &&) = default;
-    Expr(const PosIdx pos) : pos(pos) {};
+    Expr(const PosIdx pos) : pos(pos)
+    {
+        ++nrExprs;
+    };
 
 public:
 
+    /**
+     * Total number of `Expr` instances ever constructed. Atomic so the
+     * parser can safely run without locking; only read by `printStatistics`.
+     */
+    static std::atomic<uint64_t> nrExprs;
+
     PosIdx pos;
 
-    Expr() = default;
+    Expr()
+    {
+        ++nrExprs;
+    }
     Expr(const Expr &) = delete;
     Expr & operator=(const Expr &) = delete;
     virtual ~Expr() { };
-- 
2.51.2

